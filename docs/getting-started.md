# Getting started with modelcontextprotocol.name

Content for the `modelcontextprotocol.name` domain — MCP-related payment facilitator listings and source.

## Install

```bash
git clone https://github.com/nirholas/modelcontextprotocol.name.git
```

## Verify the install

Clone the repository and run its checks to confirm everything works on your machine:

```bash
git clone https://github.com/nirholas/modelcontextprotocol.name.git
cd modelcontextprotocol.name
```



## Next steps

- [Examples](./examples.md) shows runnable snippets.
- The [README](https://github.com/nirholas/modelcontextprotocol.name#readme) is the complete reference.
- Found a problem? [Open an issue](https://github.com/nirholas/modelcontextprotocol.name/issues).
